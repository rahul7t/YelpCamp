<% include partials/header %>
<h1><%= campground.name%></h1>

<img src="<%=campground.image%>"></img> 

<p><%= campground.description%> </p>


<% campground.comment.forEach(function(comment){ %>
    <p>
            <strong>
                <%= comment.author %></strong> - <%= comment.text %>
            </strong>
    </p>

<% })%>
  <% include partials/footer %>